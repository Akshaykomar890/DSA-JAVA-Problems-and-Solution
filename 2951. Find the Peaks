List<Integer> ans = new ArrayList<Integer>();
        for(int i=0; i<= mountain.length-1; i++){
          if(mountain[i] > 0 && mountain[i]< mountain.length-1){
              ans.add(Math.max(mountain[i],mountain[i+1]));
          }
        }
        return ans; 
